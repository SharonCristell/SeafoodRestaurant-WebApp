@model List<Producto>

<h1>Lista de Productos</h1>

@if (Model.Count == 0) {
    <p>No hay productos registrados</p>
}

<ul class="list">
@foreach (var item in Model)
{
    <li>@item.Nombre</li>
    <li>@item.</li>
}
</ul>