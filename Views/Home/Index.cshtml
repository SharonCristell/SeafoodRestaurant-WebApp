@model List<Sucursal>

<h1>Lista de Sucursales</h1>

@if (Model.Count == 0) {
    <p>No hay sucursales registradas</p>
}

<ul class="list">
@foreach (var item in Model)
{
    <li>@item.Nombre</li>
}
</ul>